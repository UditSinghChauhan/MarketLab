{
  "name": "marketlab",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack paper trading simulator and portfolio analytics platform.",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix dashboard && npm install --prefix frontend",
    "start:backend": "npm run dev --prefix backend",
    "start:dashboard": "npm start --prefix dashboard",
    "start:frontend": "npm start --prefix frontend",
    "build": "npm run build --prefix dashboard && npm run build --prefix frontend",
    "serve:dashboard": "node scripts/serve-build.js dashboard/build 3001",
    "serve:frontend": "node scripts/serve-build.js frontend/build 3000"
  }
}
